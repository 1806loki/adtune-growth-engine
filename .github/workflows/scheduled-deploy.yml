name: One-Time Vercel Deploy

on:
  schedule:
    # May 6, 09:00 IST → 03:30 UTC
    - cron: '30 03 6 5 *'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel Deploy
        run: |
          curl -X POST ${{ secrets.VERCEL_DEPLOY_HOOK }}
